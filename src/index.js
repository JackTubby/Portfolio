import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));

alert("Hello you are about to enter Jack's Portfolio\nThe project is still very early in development but i'd love for you to see the progress\nAlso I hate .alert() to ;)");

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
